import './styles/globals.css';
export const metadata = { title: 'มิเตอร์ไฟ Home Nuy — ค่าไฟ THB' };
export default function RootLayout({ children }) {
  return (<html lang="th"><body>{children}</body></html>);
}
